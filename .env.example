# Database (Docker PostgreSQL)
DATABASE_URL="postgresql://moussy:moussy_secret@localhost:5432/moussy_campaign"

# Admin panel password (plain text, set a strong one in production)
ADMIN_PASSWORD="admin2026"

# Secret for signing session tokens (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET="your-secret-here"

# Docker compose env
POSTGRES_PASSWORD="moussy_secret"
